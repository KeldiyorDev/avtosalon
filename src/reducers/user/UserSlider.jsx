import { createSlice } from '@reduxjs/toolkit'

export const UserSlider = createSlice({
    name: 'user',
    initialState: {
        data: [],
    },
    reducers: {

    },
})
// export const { buildData } = UserSlider.actions
export default UserSlider.reducer;